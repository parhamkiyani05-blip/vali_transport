services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: vali_transport
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - vali_pg:/var/lib/postgresql/data
volumes:
  vali_pg:
